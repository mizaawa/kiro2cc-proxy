[package]
name = "kiro2cc-proxy"
version = "3.0.7"
edition = "2024"
license = "MIT"
authors = ["Harllan He"]

[profile.release]
lto = "thin"
strip = true

# Docker builds run on shared VPS instances. Avoid release LTO's high peak
# memory usage while keeping the normal release profile fully optimized.
[profile.docker]
inherits = "release"
lto = false
codegen-units = 16
debug = false
strip = true

[dependencies]
axum = "0.8"
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.12", features = ["stream", "json", "socks", "rustls-tls", "rustls-tls-native-roots"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "registry"] }
anyhow = "1.0"
http = "1.0"
futures = "0.3"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.10", features = ["v1", "v4", "fast-rng"] }
fastrand = "2"
sha2 = "0.10"
hex = "0.4"
base64 = "0.22"
crc = "3"           # CRC32C 计算
bytes = "1"         # 高效的字节缓冲区
tower-http = { version = "0.6", features = ["cors", "trace"] }
clap = { version = "4.5", features = ["derive"] }
urlencoding = "2"
parking_lot = "0.12"  # 高性能同步原语
subtle = "2.6"        # 常量时间比较（防止时序攻击）
rust-embed = "8"      # 嵌入静态文件
mime_guess = "2"      # MIME 类型推断
dirs = "5"            # 跨平台获取用户目录
ip2region = "0.1.0"
tiktoken-rs = "0.12.0"  # BPE token 估算（cl100k_base）
